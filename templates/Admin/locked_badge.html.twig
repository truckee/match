{% if not item.isLocked %}
    <a class="badge badge-success" 
       href="{{ path('lock_user', {'id':item.id}) }}">
        No</a>
{% else %}
    <a class="badge badge-danger" 
       href="{{ path('lock_user', {'id':item.id}) }}">
        Yes</a>
{% endif %}
