<nav class="navbar navbar-expand-md navbar-light" style="font-size: 1.2rem; font-weight: bold;">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <a class="navbar-brand" href="{{ path('home') }}"><img src="{{ asset('build/images/pseudo_logo.jpg') }}" alt="Community Foundation of Western Nevada"></a>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('home') }}">Home</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('vol_index') }}">Volunteer</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('nonprofit_index') }}">Nonprofits</a>
            </li>
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarAdmin" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Admin
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarAdmin">
                        <a class="dropdown-item" href="/admin">Options</a>
                    </div>
                </li>
            {% endif %}
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarAdmin" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Account
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarAccount">
                        <a class="dropdown-item" href="{{ path('reset_password') }}">Change password</a>
                            <a class="dropdown-item" href="{{ path('profile_person') }}">Edit personal profile</a>
                        {% if is_granted("ROLE_STAFF") and not is_granted('ROLE_ADMIN')%}
                            <a class="dropdown-item" href="{{ path('profile_nonprofit') }}">Edit nonprofit profile</a>
                        {% endif %}
                    </div>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>